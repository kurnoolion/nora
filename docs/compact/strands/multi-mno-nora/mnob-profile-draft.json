{
  "_DRAFT": "MNO-B profile skeleton. The MNO-B-specific fields are set per mno-b-spec.md + D-DRAFT-1..4. Fill the TODO_* values on the work PC (run `profile_debug --create` on the real PDF to auto-detect the regexes/font sizes, then merge these overrides). `content_start_section` is pending D-DRAFT-4 implementation (loader ignores it until then). `_`-prefixed keys are human notes — the profile loader ignores unknown keys.",
  "_TODO_work_pc": [
    "requirement_id.pattern: replace TODO_PREFIX with the literal common prefix from the real doc (full id shape is <PREFIX>-<PLAN>-<DIGITS>).",
    "heading_detection.numbering_pattern + levels: fill from the real section-number format / chapter+section heading fonts.",
    "body_text.font_size_min/max: fill from profile_debug --create on the real doc.",
    "content_start_section: confirm the first requirements chapter number (3 per current structure).",
    "confirm enable_table_anchored_extraction=false (requirements are body paragraphs, not table cells)."
  ],

  "profile_name": "mnob_leading_id",
  "profile_version": 1,
  "created_from": ["<MNO-B single PDF — work PC>"],
  "last_updated": "2026-06-17",

  "requirement_id": {
    "pattern": "TODO_PREFIX-[A-Z0-9]+-\\d+",
    "components": { "separator": "-", "plan_id_position": 1, "number_position": 2 },
    "anchor": "leading_text",
    "normalize": "none",
    "detection_mode": "leading_id_body"
  },

  "content_start_section": "3",

  "heading_detection": {
    "method": "numbering",
    "numbering_pattern": "TODO_section_number_regex",
    "levels": [],
    "max_observed_depth": 0
  },

  "body_text": { "font_size_min": 0.0, "font_size_max": 0.0, "font_families": [] },

  "plan_metadata": {},

  "enable_table_anchored_extraction": false,
  "ignore_strikeout": true
}
